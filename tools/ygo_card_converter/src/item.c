    else if (VarGet(VAR_YGO_SHOP) == BANLIST_YUGI_KAIBA)
    {
        if (gCardInfo[card].banYugiKaiba)
            return gCardInfo[card].banYugiKaiba;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_CRITTER)
    {
        if (gCardInfo[card].banCritter)
            return gCardInfo[card].banCritter;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_TREASURE)
    {
        if (gCardInfo[card].banTreasure)
            return gCardInfo[card].banTreasure;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_IMPERIAL)
    {
        if (gCardInfo[card].banImperial)
            return gCardInfo[card].banImperial;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_ANDROID)
    {
        if (gCardInfo[card].banAndroid)
            return gCardInfo[card].banAndroid;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_JOEY_PEGASUS)
    {
        if (gCardInfo[card].banJoeyPegasus)
            return gCardInfo[card].banJoeyPegasus;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_FIBER)
    {
        if (gCardInfo[card].banFiber)
            return gCardInfo[card].banFiber;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_YATA)
    {
        if (gCardInfo[card].banYata)
            return gCardInfo[card].banYata;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_SCIENTIST)
    {
        if (gCardInfo[card].banScientist)
            return gCardInfo[card].banScientist;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_VAMPIRE)
    {
        if (gCardInfo[card].banVampire)
            return gCardInfo[card].banVampire;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_CHAOS)
    {
        if (gCardInfo[card].banChaos)
            return gCardInfo[card].banChaos;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_WARRIOR)
    {
        if (gCardInfo[card].banWarrior)
            return gCardInfo[card].banWarrior;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_GOAT)
    {
        if (gCardInfo[card].banGoat)
            return gCardInfo[card].banGoat;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_CYBER)
    {
        if (gCardInfo[card].banCyber)
            return gCardInfo[card].banCyber;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_REAPER)
    {
        if (gCardInfo[card].banReaper)
            return gCardInfo[card].banReaper;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_CHAOS_RETURN)
    {
        if (gCardInfo[card].banChaosReturn)
            return gCardInfo[card].banChaosReturn;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_DEMISE)
    {
        if (gCardInfo[card].banDemise)
            return gCardInfo[card].banDemise;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_TROOPER)
    {
        if (gCardInfo[card].banTrooper)
            return gCardInfo[card].banTrooper;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_ZOMBIE)
    {
        if (gCardInfo[card].banZombie)
            return gCardInfo[card].banZombie;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_PERFECT_CIRCLE)
    {
        if (gCardInfo[card].banPerfectCircle)
            return gCardInfo[card].banPerfectCircle;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_DAD_RETURN)
    {
        if (gCardInfo[card].banDADReturn)
            return gCardInfo[card].banDADReturn;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_GLADIATOR)
    {
        if (gCardInfo[card].banGladiator)
            return gCardInfo[card].banGladiator;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_TELEDAD)
    {
        if (gCardInfo[card].banTeleDAD)
            return gCardInfo[card].banTeleDAD;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_CAT)
    {
        if (gCardInfo[card].banCat)
            return gCardInfo[card].banCat;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_EDISON)
    {
        if (gCardInfo[card].banEdison)
            return gCardInfo[card].banEdison;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_FROG)
    {
        if (gCardInfo[card].banFrog)
            return gCardInfo[card].banFrog;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_STARSTRIKE)
    {
        if (gCardInfo[card].banStarstrike)
            return gCardInfo[card].banStarstrike;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_TENGU)
    {
        if (gCardInfo[card].banTengu)
            return gCardInfo[card].banTengu;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_DINO_RABBIT)
    {
        if (gCardInfo[card].banDinoRabbit)
            return gCardInfo[card].banDinoRabbit;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_WIND_UP)
    {
        if (gCardInfo[card].banWindUp)
            return gCardInfo[card].banWindUp;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_MIAMI)
    {
        if (gCardInfo[card].banMiami)
            return gCardInfo[card].banMiami;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_MEADOWLANDS)
    {
        if (gCardInfo[card].banMeadowlands)
            return gCardInfo[card].banMeadowlands;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_BABY_RULER)
    {
        if (gCardInfo[card].banBabyRuler)
            return gCardInfo[card].banBabyRuler;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_RAVINE_RULER)
    {
        if (gCardInfo[card].banRavineRuler)
            return gCardInfo[card].banRavineRuler;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_FIRE_WATER)
    {
        if (gCardInfo[card].banFireWater)
            return gCardInfo[card].banFireWater;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_HAT)
    {
        if (gCardInfo[card].banHAT)
            return gCardInfo[card].banHAT;
        else
            return 3;
    }
    else if (VarGet(VAR_YGO_SHOP) == BANLIST_VEGAS)
    {
        if (gCardInfo[card].banVegas)
            return gCardInfo[card].banVegas;
        else
            return 3;
    }
